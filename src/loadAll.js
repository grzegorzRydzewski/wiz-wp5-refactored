//const imagesJSON = require('./images.json');

export default function loadAll(r) {
  let images = {};
  const tileWidth = 32;
  const tileHeight = 32;
  r.keys().forEach((item, index) => { 
    //separating to small canvases
    
   // images[item.replace('./', '')] =  r(item);
   // for( let x = 0; x < 100; x + 32 ) {
    
   // }
    
  });
  return images;
 }
